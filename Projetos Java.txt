001-meuPrimeiroProjeto

package meuprimeiroprojeto;
                               //Respeite a formatação dos comandos!

public class App {
                           //Metodo "main", onde inicia o código.
    public static void main(String[] args) {
        System.out.println("Hello World!");   //Comando de escrita em Java.
        System.out.println("Meu primeiro projetooo eeee :D");
        System.out.println("Aqui começamoss >:) ");
        
    }
}
--------------------------------------------------------------
002-comentarios
package comentarios;


public class App {

    public static void main(String[] args) {
        System.out.println("Hello World!");
        
        //Cometário em linha
        /*Bloco de comentárioo wewewewewe.
            Para comentários maiores :)  
        */
        /** Comentário de documentação (Vai pro JavaDoc)
         * Aqui eu posso colocar documentoss :D
         * Ou transformar o que tem aqui em documentos :) 
        Exemplo: JavaDoc 
         */
        
    }
}

--------------------------------------------------------------
003-comandosDeLeitura
package com.mycompany.comandosdeleitura;

import java.util.Scanner;

public class App {

    public static void main(String[] args) {
        
        Scanner leitor = new Scanner(System.in);
        
        //Lendo dados
        int idade = leitor.nextInt(); //Comando guarda dentor da variável
        float cotacaoDolar = leitor.nextFloat(); 
        double cotacaoEuro = leitor.nextDouble(); 
        String nome = leitor.nextLine(); //Lê uma frase inteira
        String codigoRua = leitor.next(); //Lê apenas uma palavra
        
        //Exibindo dados
        System.out.println("Texto que será exibido no console.");
        //Pula pra próxima linha.
        
        System.out.print("Texto que será exibido no console.");
        //Fica na mesma linha.  
     }
}

--------------------------------------------------------------
004-variaveisTiposOperadores
package variaveistiposoperadores;


public class VariaveisTiposOperadores {

    public static void main(String[] args) {
       
        /*Deve começar com uma letra minúscula!
        - Nas variáveis Float e Double sempre coloque a primeira letra 
        correspondente no final da definição da variável.
        - Na variável char tem aspas simples e na String aspas duplas.
        */
        
        //Variáveis primitivas
        int idade = 19; 
        float cotacaoDolar = 0,09f; //Armazenar números reais.
        double cotacaoEuro = 10,00d;//números com casas decimais
        char genero = 'M'; //Armazena apenas uma letra
        byte ponto = 0; //numérica inteiro
        boolean estaCadastrado = false; //Do tipo verdadeiro ou falso
        
        //Não primitivo
        String nome = "Esse é um texto em uma variável String";
        
        /*Operações aritmeticas     *Respeitar as regras*
        + Adição
        - Subtração
        / Divisão
        * Multiplicação
        % Resto 
        */
                
            
    }
}
--------------------------------------------------------------
005-estruturaCondicionalSe
package estruturacondicionalse;

public class App {

    public static void main(String[] args) {
      int media = 7;  
          
        /*OPERADORES RELACIONAIS
        >  Maior
        >= Maior ou igual
        <  Menor
        <= Menor ou igual
        == Comparando
        =  Atribuindo
        != Diferente
          OPERADORES LÓGICOS
        && - E
        || - OU
        !  - NÃO 
        */        
        
        if(media >= 7){
           if(media == 10) {
                //APROVADO COM NOTA MÁXIMA!
           } else {
                //APROVADO!
           }
        } else {
           //REPROVADO!
        }
        
    }
}

--------------------------------------------------------------
006-estruturaSwitchEscolha
package estruturaswitchescolha;

public class EstruturaSwitchEscolha {

    public static void main(String[] args) {
      
        //Com a estrutra escolha temos várias opções :D
        
        int codigoProduto = 2;
        
        switch(codigoProduto){   //Para cada caso, um break
        
            case 1:
                
                break;
            
            case 2: 
                
                break;
              //Caso nada atenda ao resultado desejado, então use:
            default:    
            
        }
    }
}

--------------------------------------------------------------
007-estruturafor
package estruturafor;


public class App {

    public static void main(String[] args) {
        
        //Primeira parte: Variável de controle.
        //Segunda parte: Condição de parada. 
        //Terceira parte: Incremento
        for(int i = 0; i <= 10; i++){
            
            
        }
        
    }
}

--------------------------------------------------------------
008-estruturaWhile
package estruturawhile;

import java.util.Scanner;

public class App {

    public static void main(String[] args) {

        /*Em While não sabemos o ponto de parada, a parada depende
        de algo que está dentro do algoritmo que está sendo repetido.
        A condição de parada deve ser lógica.
         */
        int totalAlunos = 10;

        Scanner leitorScanner = new Scanner(System.in);

        while (totalAlunos > 0) {

            String nomeAluno = leitorScanner.nextLine();
            int idadeAluno = leitorScanner.nextInt();
            
            System.out.println("O nome do aluno é: " + nomeAluno + " e sua "
                    +"idade é: " + idadeAluno);
        
            totalAlunos = totalAlunos - 1;
            
            //Cuidar para que em algum momento o algoritmo fique FALSO.
            //Assim o While terá seu ponto de parada.
        }
    }
}

--------------------------------------------------------------
009-exercicioEstruturaRepeticao
package exercicioestruturarepeticao;

import java.util.Scanner;

public class App {

    public static void main(String[] args) {
       
      Scanner leitorScanner = new Scanner(System.in);
      
      char desejaContinuar = 'S'; //Aspas simples :).
                                 //Atribuir valor a variável CHAR, aspas duplas 
      while (desejaContinuar == 'S' || desejaContinuar == 's'){
      
          System.out.println("Digite um número");
          int numero;
          numero = leitorScanner.nextInt();
          
          if(numero == 0){
            System.out.println("O número é zero.");
          }else{
             if(numero >0){
               System.out.println("O número é maior que zero.");               
             }else{
               System.out.println("O número é menor que zero.");
          }
        }
          System.out.println("Deseja continuar? S - Sim / N - Não");
          desejaContinuar = leitorScanner.next().charAt(0);
      
      }
           
    }
}
--------------------------------------------------------------













